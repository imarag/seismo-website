---
// Stack.astro
const { size = "md", class: className = "" } = Astro.props;

const sizeMap = {
  sm: "gap-2",
  md: "gap-4",
  lg: "gap-8",
  xl: "gap-12",
};

const spacing = sizeMap[size] ?? sizeMap.md;
---

<div class={`flex flex-col items-stretch ${spacing} ${className}`}>
  <slot />
</div>
