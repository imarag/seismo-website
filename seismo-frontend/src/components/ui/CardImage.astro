---
import Img from "../ui/Img.astro";
const { src, alt, class: className = "", ...attrs } = Astro.props;
const globalClass = `w-32 block mx-auto ${className}`;
---

<Img src={src} alt={alt} class={globalClass} {...attrs} />
